# Base URL of hapto-api. Use your machine's LAN IP, not localhost, when
# testing on a physical Android device, since localhost on the phone means
# the phone itself, not your dev machine.
API_BASE_URL=http://192.168.1.X:8080
